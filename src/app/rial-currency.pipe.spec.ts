import { RialCurrencyPipe } from './rial-currency.pipe';

describe('RialCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new RialCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
